export function calculatePadding(): void {
  // There's no actual support for padding, so we fake it by giving extra margin
  // to the children. But if there's multiple children, we'd need a wrapper
  // element
}
